pluginManagement {
    includeBuild("build-logic")
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
includeBuild 'codegen'
rootProject.name = "finazon-grpc-java"
include('lib', 'examples:tickers', 'examples:timeSeries')