plugins {
    id "java"
    id "application"
    id "idea"
}

repositories {
    mavenCentral()
}

dependencies {
    implementation "io.finazon:finazon-grpc-java:${version}"
    runtimeOnly "io.grpc:grpc-netty-shaded:${grpcVersion}"
}